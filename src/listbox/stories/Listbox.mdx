import { ArgTypes, Canvas, Controls, Meta, Primary, Stories, Subtitle, Title } from "@storybook/addon-docs/blocks";
import * as ListboxStories from "./Listbox.stories";

<Meta
	of={ListboxStories}
	parameters={{
		docs: {
			toc: true
		}
	}}
/>

<Title>Listbox</Title>
<Subtitle>Список выбора для single- и multi-select сценариев с кастомным рендером элементов</Subtitle>

Listbox уже имеет живые stories, а эта страница собирает их в структурированную документацию с краткими рекомендациями, примерами подключения и API.
Ниже можно быстро проверить базовый сценарий, а затем перейти к отдельным режимам использования и состояниям компонента.

## Основной сценарий

<Primary />

<Controls of={ListboxStories.SingleSelect} />

```tsx
import { useState } from "react";

import { Listbox } from "@/shared/ui";

const items = ["Черновик", "В работе", "Завершен"];

export function Example() {
	const [value, setValue] = useState(undefined);

	return <Listbox items={items} value={value} onChange={setValue} />;
}
```

## Практические рекомендации

- Начните с базового сценария ниже, а затем проверьте в stories те состояния, которые реально используются в вашем экране.

## Сценарии использования

### Выбор одного значения

Выбор одного значения.

<Canvas of={ListboxStories.SingleSelect} />

### Выбор нескольких значений

Выбор нескольких значений.

<Canvas of={ListboxStories.MultiSelect} />

### Кастомный рендер элементов списка

Кастомный рендер элементов списка.

<Canvas of={ListboxStories.CustomRender} />

## API

<ArgTypes of={ListboxStories} />

## Все сторисы

<Stories includePrimary={false} />
