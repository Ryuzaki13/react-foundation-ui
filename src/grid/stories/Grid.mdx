import { ArgTypes, Canvas, Controls, Meta, Primary, Stories, Subtitle, Title } from "@storybook/addon-docs/blocks";
import * as GridStories from "./Grid.stories";

<Meta
	of={GridStories}
	parameters={{
		docs: {
			toc: true
		}
	}}
/>

<Title>Grid</Title>
<Subtitle>Композиция для построения grid-раскладок с управлением колонками, строками и позиционированием элементов</Subtitle>

Grid уже имеет живые stories, а эта страница собирает их в структурированную документацию с краткими рекомендациями, примерами подключения и API.
Ниже можно быстро проверить базовый сценарий, а затем перейти к отдельным режимам использования и состояниям компонента.

## Основной сценарий

<Primary />

<Controls of={GridStories.BasicGrid} />

```tsx
import { Grid } from "@/shared/ui";

export function Example() {
	return (
		<Grid.Container templateColumns="repeat(2, minmax(0, 1fr))" gap="md">
			<Grid.Item>Левая колонка</Grid.Item>
			<Grid.Item>Правая колонка</Grid.Item>
		</Grid.Container>
	);
}
```

## Практические рекомендации

- Начните с базового сценария ниже, а затем проверьте в stories те состояния, которые реально используются в вашем экране.

## Сценарии использования

### Минимальный пример построения grid-раскладки

Минимальный пример построения grid-раскладки.

<Canvas of={GridStories.BasicGrid} />

### Автоматическое размещение элементов в сетке

Автоматическое размещение элементов в сетке.

<Canvas of={GridStories.AutoFlow} />

### Выравнивание содержимого и элементов контейнера

Выравнивание содержимого и элементов контейнера.

<Canvas of={GridStories.Alignment} />

### Сравнение интервалов между элементами

Сравнение интервалов между элементами.

<Canvas of={GridStories.Gaps} />

### Применение готовых grid-вариантов

Применение готовых grid-вариантов.

<Canvas of={GridStories.PredefinedVariants} />

### Использование сложных шаблонов строк и колонок

Использование сложных шаблонов строк и колонок.

<Canvas of={GridStories.ComplexTemplates} />

### Работа с именованными областями раскладки

Работа с именованными областями раскладки.

<Canvas of={GridStories.GridAreas} />

### Ручное позиционирование отдельных элементов

Ручное позиционирование отдельных элементов.

<Canvas of={GridStories.ItemPlacement} />

### Режим inline-grid в потоке документа

Режим inline-grid в потоке документа.

<Canvas of={GridStories.InlineGrid} />

### Прикладной пример использования в интерфейсе

Прикладной пример использования в интерфейсе.

<Canvas of={GridStories.RealWorld} />

### Адаптация раскладки под ширину контейнера

Адаптация раскладки под ширину контейнера.

<Canvas of={GridStories.Responsive} />

## API

<ArgTypes of={GridStories} />

## Все сторисы

<Stories includePrimary={false} />
