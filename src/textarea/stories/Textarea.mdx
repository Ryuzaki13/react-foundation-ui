import { ArgTypes, Canvas, Controls, Meta, Primary, Stories, Subtitle, Title } from "@storybook/addon-docs/blocks";
import * as TextareaStories from "./Textarea.stories";

<Meta
	of={TextareaStories}
	parameters={{
		docs: {
			toc: true
		}
	}}
/>

<Title>Textarea</Title>
<Subtitle>Многострочное текстовое поле для описаний, комментариев и заметок</Subtitle>

Textarea уже имеет живые stories, а эта страница собирает их в структурированную документацию с краткими рекомендациями, примерами подключения и API.
Ниже можно быстро проверить базовый сценарий, а затем перейти к отдельным режимам использования и состояниям компонента. Отдельно разобраны контролируемые сценарии, заблокированные состояния.

## Основной сценарий

<Primary />

<Controls of={TextareaStories.Controlled} />

```tsx
import { useState } from "react";

import { Textarea } from "@/shared/ui";

export function Example() {
	const [value, setValue] = useState("");

	return <Textarea label="Комментарий" value={value} onChange={setValue} placeholder="Введите текст" />;
}
```

## Практические рекомендации

- Если состояние должно жить во внешней форме или модели, ориентируйтесь на контролируемый режим из документации.
- Заблокированное состояние лучше проверять отдельно, чтобы убедиться, что визуально и поведенчески оно остается понятным.

## Сценарии использования

### Компонент работает в контролируемом режиме, когда состояние хранится во внешнем компоненте

Компонент работает в контролируемом режиме, когда состояние хранится во внешнем компоненте.

<Canvas of={TextareaStories.Controlled} />

### Компонент с уже заданным значением

Компонент с уже заданным значением.

<Canvas of={TextareaStories.WithValue} />

### Проверка поведения в заблокированном состоянии

Проверка поведения в заблокированном состоянии.

<Canvas of={TextareaStories.Disabled} />

## API

<ArgTypes of={TextareaStories} />

## Все сторисы

<Stories includePrimary={false} />
