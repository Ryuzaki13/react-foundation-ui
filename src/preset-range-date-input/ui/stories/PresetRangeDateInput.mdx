import { ArgTypes, Canvas, Controls, Meta, Primary, Stories, Subtitle, Title } from "@storybook/addon-docs/blocks";
import * as PresetRangeDateInputStories from "./PresetRangeDateInput.stories";

<Meta
	of={PresetRangeDateInputStories}
	parameters={{
		docs: {
			toc: true
		}
	}}
/>

<Title>PresetRangeDateInput</Title>
<Subtitle>Составной контрол диапазона дат с синхронизацией ручного ввода и готовых пресетов</Subtitle>

PresetRangeDateInput уже имеет живые stories, а эта страница собирает их в структурированную документацию с краткими рекомендациями, примерами подключения и API.
Ниже можно быстро проверить базовый сценарий, а затем перейти к отдельным режимам использования и состояниям компонента.

## Основной сценарий

<Primary />

<Controls of={PresetRangeDateInputStories.Default} />

```tsx
import { useState } from "react";

import { PresetRangeDateInput } from "@/shared/ui";

export function Example() {
	const [range, setRange] = useState(null);
	const [presetId, setPresetId] = useState(null);

	return <PresetRangeDateInput value={range} onChange={setRange} presetId={presetId} onPresetIdChange={setPresetId} />;
}
```

## Практические рекомендации

- Если компонент участвует в фильтрации или поиске, заранее определите дефолтные значения и допустимые ограничения.

## Сценарии использования

### Сценарий со стандартным поведением и дефолтными настройками

Сценарий со стандартным поведением и дефолтными настройками.

<Canvas of={PresetRangeDateInputStories.Default} />

## API

<ArgTypes of={PresetRangeDateInputStories} />

## Все сторисы

<Stories includePrimary={false} />
